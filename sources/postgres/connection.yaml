name: postgres
type: postgres
options:
  # -----------------------------------------------------------
  # DOCKER DEFAULTS
  # These allow 'docker-compose up' to work instantly.
  # They are automatically overridden if the specific ENV vars exist.
  # Evidence automatically looks for environment variables following this pattern:
  # EVIDENCE_SOURCE__[source_name]__[option_name]
  # -----------------------------------------------------------
  host: "postgres"        # Matches the docker-compose service name
  port: 5432
  database: "amos_db"
  schema: "public_metrics" 
  ssl: false
  user: "amos"            # Credentials for development use only in Docker environment
  password: "password"    # Credentials for development use only in Docker environment